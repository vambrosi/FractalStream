viewer:
  title: Douady rabbit
  style: complex-plane
  size: 400x425
  resizable: true
  z-coord: z
  initial-center: 0
  initial-pixel-size: 1/128
  code: |
    # Set our loop counter to 0
    k : ℤ ⭠ 0

    # Iterate until we escape or loop 100 times
    while |z| < 10 and k < 100
      z ⭠ z² + -0.12256 + 0.74486 𝑖
      k ⭠ k + 1

    # If we didn't escape in 100 loops, draw a black
    # point. Otherwise, draw a white point.
    if k = 100 then
      output black to color
    else
      output white to color
